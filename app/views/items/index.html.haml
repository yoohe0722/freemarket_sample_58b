= render 'module/header'
.wrapper__top-page
  .top-page__banners


  .section
    .section__header
      %h2 人気のカテゴリー
      .section__links
        %ul
          %li<
            = link_to 'レディース', '#category_1'
          %li<
            = link_to 'メンズ', '#category_2'
          %li<
            = link_to '家電・スマホ・カメラ', '#category_3'
          %li<
            = link_to 'おもちゃ・ホビー・グッズ', '#category_4'

    #category_1
      .categories__container
        .container__title
          %h3 レディース新着アイテム
          = link_to('もっと見る','#', class: 'view_more')
        .container__item-box
          .item__grid.clearfix
            - @item.each do |item|
              = render partial: "index", locals: { item: item }

    #category_2
      .categories__container
        .container__title
          %h3 メンズ新着アイテム
          = link_to('もっと見る','#', class: 'view_more')
        .container__item-box
          .item__grid.clearfix
            - @item.each do |item|
              = render partial: "index", locals: { item: item }

    #category_3
      .categories__container
        .container__title
          %h3 家電・スマホ・カメラ
          = link_to('もっと見る','#', class: 'view_more')
        .container__item-box
          .item__grid.clearfix
            - @item.each do |item|
              = render partial: "index", locals: { item: item }

    #category_4
      .categories__container
        .container__title
          %h3 おもちゃ・ホビー・グッズ
          = link_to('もっと見る','#', class: 'view_more')
        .container__item-box
          .item__grid.clearfix
            - @item.each do |item|
              = render partial: "index", locals: { item: item }

= render 'module/footer'
= render 'module/sell-btn'

